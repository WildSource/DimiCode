package dataTypes;

import lexer.Token;

public interface TokenDatatypeTable {
    Token operatorPlus = new Token("+" );
    Token operatorMinus = new Token("-" );
    Token operatorMultiply = new Token("*" );
    Token operatorDivide = new Token("/" );
}
